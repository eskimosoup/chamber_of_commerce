<section class="content-area">
  <div class="row">
    <div class="medium-8 small-12 columns">
      <div class="main-content-wrap">
        <h1>Edit Offer</h1>

        <p>Discard changes and go back to <%= link_to 'your profile', edit_members_path %>.</p>

        <div class="content">
          <%= render 'form' %>

          <%= link_to 'Delete', member_member_offer_path(@member, @member_offer), method: :delete, data: { confirm: 'Are you sure you want to delete this?' }, class: 'content-box-ghost-button right' %>
        </div>
      </div>
    </div>

    <div class="medium-4 small-12 columns">
      <%= render 'shared/sidebar' %>
    </div>
  </div>
</section>
