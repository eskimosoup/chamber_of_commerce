<% present package do |presenter| %>
<div class="membership-package">
  <div class="membership-package__title">
    <%= presenter.title %>
  </div>

  <div class="membership-package__cost">
    <%= presenter.cost %> + VAT
  </div>
</div>
<% end %>
