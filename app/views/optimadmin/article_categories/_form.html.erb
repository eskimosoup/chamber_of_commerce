<%= form_for(article_category) do |f| %>
<% if article_category.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(article_category.errors.count, "error") %> prohibited this category from being saved:</h2>

    <ul>
      <% article_category.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>


  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :member_related %><br/>
    <%= f.check_box :member_related %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
